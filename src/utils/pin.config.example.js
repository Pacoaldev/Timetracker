/**
 * Copia este archivo como pin.config.js y pon tu hash SHA-256:
 *
 *   node -e "const c=require('crypto'); console.log(c.createHash('sha256').update('TU_PIN').digest('hex'))"
 *
 * pin.config.js está en .gitignore y no se sube al repositorio.
 */
export const MASTER_PIN_HASH = 'REEMPLAZA_CON_TU_HASH_SHA256'
